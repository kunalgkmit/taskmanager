import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    backgroundColor: 'yellow',
    borderWidth: 1,
    borderRadius: 20,
    padding: 10,
    alignItems: 'center',
    margin: 3,
  },
  text: {
    fontWeight: 'bold',
  },
});
